services:
  # Web Service (Spring Boot Application)
  - type: web
    name: sistema-voos
    env: docker
    region: oregon
    plan: free
    dockerfilePath: ./Dockerfile
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: sistema-voos-db
          property: connectionString
      - key: DB_USERNAME
        value: adrian
      - key: DB_PASSWORD
        value: 71fULydrMH3a4XH4EbMExkqPC0mYZmBG
      - key: JWT_SECRET
        generateValue: true
      - key: JAVA_OPTS
        value: "-Xmx512m -Xms256m"

databases:
  # PostgreSQL Database (referência ao banco existente)
  - name: sistema-voos-db
    databaseName: sistema_voos_db
    user: adrian
    region: oregon
    plan: free
